# django-base
```bash
git clone git@github.com:3westrick/django-base.git
mv django-base project_name
cd project_name
chmod +x entrypoint.sh
docker build -t my-django-image . 
docker compose up -d 
```
